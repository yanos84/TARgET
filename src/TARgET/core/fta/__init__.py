"""
Finite tree automata.

This package provides the core classes for representing finite tree automata
(FTAs) and weighted finite tree automata (WFTAs). It defines the fundamental
data structures required to construct, manipulate, and analyze automata over
ranked alphabets.

Modules:
    -abst_fta: Abstract base classes defining the common interface for finite tree automata.
    -rankedfta: Implementation of ranked finite tree automata.
    -rule: Abstract representation of transition rules.
    -rankedRule: Transition rules for ranked finite tree automata.
    -state: State representations used by finite tree automata.
"""